import type { PillarSectionConfig } from '../../config/module';

const config: PillarSectionConfig = {
  kind: 'pillar',
  parent: null,
  id: 'soul',
  title: 'Soul',
  description:
    'Walk the classical paths: karma yoga, gyan yoga, bhakti yoga, ashtanga and tantra.',
  routePrefix: '/soul/',
  enabled: true,
  accent: 'plum',
};

export default config;
