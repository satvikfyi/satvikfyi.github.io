import type { PillarSectionConfig } from '../../config/module';

const config: PillarSectionConfig = {
  kind: 'pillar',
  parent: null,
  id: 'mind',
  title: 'Mind',
  description:
    'Steady and clarify the mental sheath: pranayama, meditation and mantra.',
  routePrefix: '/mind/',
  enabled: true,
  accent: 'indigo',
};

export default config;
