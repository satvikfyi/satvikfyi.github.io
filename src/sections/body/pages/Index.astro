---
/**
 * Body pillar landing, registry-driven; auto-lists enabled body modules.
 */
import PillarLanding from '../../../shared/components/PillarLanding.astro';
import config from '../module.config';
---

<PillarLanding pillar={config} />
