import type { PillarSectionConfig } from '../../config/module';

const config: PillarSectionConfig = {
  kind: 'pillar',
  parent: null,
  id: 'body',
  title: 'Body',
  description:
    'Nourish and care for the physical sheath: satvik meals, yogasana and ayurvedic wisdom.',
  routePrefix: '/body/',
  enabled: true,
  accent: 'saffron',
};

export default config;
